import React from "react";

export type ContextItems = {
  key: Number,
  title: string,
}

export default function MenuItem(
  { key: Number, title: string }: ContextItems
) {
  return (
    <div>

    </div>
  );
}